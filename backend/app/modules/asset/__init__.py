"""Asset module — domain layer for real estate objects (parcels, listings, ...).

MVP: only `Asset` model + CRUD endpoint. See docs/domain-model.md for the
planned set of entities (Parcel, Listing, Auction, Restriction, Opportunity).
"""
